LLM_API_KEY=ollama
ANTHROPIC_API_KEY=your_key_if_switching_to_claude_api
# Optional: absolute path to a code repo you want auto-ingested (git hook /
# ingest scripts). Leave commented to skip code ingestion.
# MEMCON_CODE_DIR=/absolute/path/to/your/repo
# Optional Hugging Face token for faster model downloads (not required).
HF_TOKEN=
